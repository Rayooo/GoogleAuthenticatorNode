docker ps 显示正在运行的容器
docker ps -a 显示所有容器，包括暂停的

docker images 显示镜像

docker rm 容器名  删除某个容器
docker rmi 镜像名  删除某个镜像

sudo docker run -p 80:3000 -d node-app(镜像名)  启动镜像，并且映射端口 -d 后台运行

sudo docker start node-app(暂停的容器名) 启动暂停的容器